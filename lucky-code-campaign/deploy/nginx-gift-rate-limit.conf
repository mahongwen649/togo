# Add these directives in the nginx http context, outside all server blocks.
limit_req_zone $binary_remote_addr zone=gift_claim:10m rate=5r/m;
limit_req_zone $binary_remote_addr zone=gift_admin_login:10m rate=3r/m;
