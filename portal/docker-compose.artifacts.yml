services:
  portal-api:
    image: portal-portal-api:latest

  portal-web:
    image: ${PORTAL_NGINX_IMAGE:-nginx:1.27-alpine}
    volumes:
      - ./deploy/portal-web.nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - ./backend/internal/web/dist:/usr/share/nginx/html:ro
